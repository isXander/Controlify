//? if neoforge {
/*package dev.isxander.controlify.platform.neoforge.mixins;

import dev.isxander.controlify.screenop.ScreenProcessor;
import dev.isxander.controlify.screenop.ScreenProcessorProvider;
import dev.isxander.controlify.screenop.compat.neoforge.ModListScreenProcessor;
import net.neoforged.neoforge.client.gui.ModListScreen;
import org.spongepowered.asm.mixin.Mixin;
import org.spongepowered.asm.mixin.Unique;

@Mixin(ModListScreen.class)
public class ModListScreenMixin implements ScreenProcessorProvider {
    @Unique
    private final ScreenProcessor<?> processor =
            new ModListScreenProcessor((ModListScreen) (Object) this);

    @Override
    public ScreenProcessor<?> screenProcessor() {
        return this.processor;
    }
}*/
//?}
